// Code generated by https://github.com/go-dev-frame/sponge

package routers

import (
	"github.com/gin-gonic/gin"

	"github.com/go-dev-frame/sponge/pkg/logger"
	//"github.com/go-dev-frame/sponge/pkg/middleware"

	storeV1 "store/api/store/v1"
	"store/internal/handler"
)

func init() {
	allMiddlewareFns = append(allMiddlewareFns, func(c *middlewareConfig) {
		productMediaMiddlewares(c)
	})

	allRouteFns = append(allRouteFns,
		func(r *gin.Engine, groupPathMiddlewares map[string][]gin.HandlerFunc, singlePathMiddlewares map[string][]gin.HandlerFunc) {
			productMediaRouter(r, groupPathMiddlewares, singlePathMiddlewares, handler.NewProductMediaHandler())
		})
}

func productMediaRouter(
	r *gin.Engine,
	groupPathMiddlewares map[string][]gin.HandlerFunc,
	singlePathMiddlewares map[string][]gin.HandlerFunc,
	iService storeV1.ProductMediaLogicer) {
	storeV1.RegisterProductMediaRouter(
		r,
		groupPathMiddlewares,
		singlePathMiddlewares,
		iService,
		storeV1.WithProductMediaLogger(logger.Get()),
		storeV1.WithProductMediaHTTPResponse(),
		storeV1.WithProductMediaErrorToHTTPCode(
		// Set some error codes to standard http return codes,
		// by default there is already ecode.InternalServerError and ecode.ServiceUnavailable
		// example:
		// 	ecode.Forbidden, ecode.LimitExceed,
		),
	)
}

// you can set the middleware of a route group, or set the middleware of a single route,
// or you can mix them, pay attention to the duplication of middleware when mixing them,
// it is recommended to set the middleware of a single route in preference
func productMediaMiddlewares(c *middlewareConfig) {
	// set up group route middleware, group path is left prefix rules,
	// if the left prefix is hit, the middleware will take effect, e.g. group route is /api/v1, route /api/v1/productMedia/:id  will take effect
	// c.setGroupPath("/api/v1/productMedia", middleware.Auth())

	// set up single route middleware, just uncomment the code and fill in the middlewares, nothing else needs to be changed
	//c.setSinglePath("POST", "/api/v1/productMedia", middleware.Auth())    // Create productMedia
	//c.setSinglePath("DELETE", "/api/v1/productMedia/:id", middleware.Auth())    // DeleteByID delete productMedia by id
	//c.setSinglePath("PUT", "/api/v1/productMedia/:id", middleware.Auth())    // UpdateByID update productMedia by id
	//c.setSinglePath("GET", "/api/v1/productMedia/:id", middleware.Auth())    // GetByID get productMedia by id
	//c.setSinglePath("POST", "/api/v1/productMedia/list", middleware.Auth())    // List of productMedia by query parameters
}
